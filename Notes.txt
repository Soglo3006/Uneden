Admin access preparation:
- Frontend `/admin/support` is gated by admin check.
- Configure admin emails in frontend via `NEXT_PUBLIC_ADMIN_EMAILS` (comma-separated).
- Backend requires admin for listing/updating support tickets. Set `ADMIN_EMAILS` (comma-separated) in environment to allow specific emails, or set `role=admin` in Supabase user/app metadata.
MVP FAIT POUR FIELDHEARTS:

Backend:

- User/login - Oui
- Profile creation et editing - Oui
- Search et discovery/ location-based search - Non
- Create/ list a service - OUI
- Booking + escrow payment - OUI
- in app messaging - NON
-rating et reviews - OUI
- admin dashboard - NON
- Dispute system - OUI
-Notification - NON


Frontend: 
Les pages a faire:
-Home
-Listings
-Listing Details
-Create Listing
-Register
-Login
-User Dashboard
-User Profile
-Edit Profile